templates:
  - name: inputTemplate
  - components:
      parent: input
      children: Input
      - name: Input
        type: Input
        props:
          input: "input"
          name: "{{ name }}" 
          type: "{{ inputType }}"
          placeholder: "{{ placeholder }}"
