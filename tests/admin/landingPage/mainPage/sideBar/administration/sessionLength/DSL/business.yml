resource: chat/session-length
description: "Buerokratt-Chatbot"
test:
  main:
  heading:
    args:
      - type: text
      - value: "Session length"
      - style: h1

    card:
      name: card__body
      label: 
      args:
        - type: text
        - value: "Session length"
        - style: label

      input:
        label: "Session length"
        args:
            - type: number
            - style: input
            - adjacentTo: label

      name: card__footer
      button:
        args:
            - type: button
            - value: "Save"
            - style: button
