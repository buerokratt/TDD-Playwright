{
  "name": "pw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:test": "cross-env ENV=test npx playwright test",
    "test:test:smoke": "cross-env ENV=test npx playwright test --project setup --project smoke",
    "test:stage": "cross-env ENV=stage npx playwright test",
    "test:stage:smoke": "cross-env ENV=stage npx playwright test --project setup --project smoke",
    "test:test:debug": "cross-env ENV=test npx playwright test --debug",
    "test:stage:debug": "cross-env ENV=stage npx playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^22.3.0",
    "cross-env": "^10.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "js-yaml": "^4.1.0",
    "openai": "^4.68.4"
  }
}
